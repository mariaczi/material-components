import bindBoolean from './bind-boolean';
import bindRaw from './bind-raw';
import bindClickAway from './click-away';
import field from './field';
import slot from './slot';
import waveEffect from './wave-effect';

var directives: any = {
    bindBoolean,
    bindRaw,
    bindClickAway,
    field,
    slot,
    waveEffect
};

export default directives;
